import 'package:flutter/material.dart';

import '../../widgets/drawer.dart';

class MisCertificadosScreen extends StatelessWidget {
  const MisCertificadosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MisCertificadosScreen'),
      ),
    );
  }
}
