import 'package:aulasapp/widgets/drawer.dart';
import 'package:flutter/material.dart';

class BibliotecaDeVideosScreen extends StatelessWidget {
  const BibliotecaDeVideosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('BibliotecaDeVideosScreen'),
      ),
    );
  }
}
